# ========================================
# Campus5 Mobile App - Production Environment Configuration
# ========================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Fill in all the real values (remove the placeholder text)
# 3. NEVER commit .env.production to Git (it's in .gitignore)
# 4. Keep your production credentials secure
#
# ========================================

# ----------------------------------------
# Supabase Configuration (REQUIRED)
# ----------------------------------------
# These are required for the app to function
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
#
# The Supabase project URL (e.g., https://abcdefghijk.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# The Supabase anonymous/public key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ----------------------------------------
# Feature Flags (OPTIONAL)
# ----------------------------------------
# Enable testing mode for dating feature (allows re-matching with same users)
# Set to "true" to enable, "false" or omit to disable
# WARNING: Only enable this in development/testing environments!
NEXT_PUBLIC_ENABLE_DATING_TEST=false

# ----------------------------------------
# App Configuration (OPTIONAL)
# ----------------------------------------
# App environment (development, staging, production)
NEXT_PUBLIC_APP_ENV=production

# App version (should match package.json version)
NEXT_PUBLIC_APP_VERSION=1.0.0

# ----------------------------------------
# Analytics & Monitoring (OPTIONAL)
# ----------------------------------------
# Add your analytics service keys here as needed
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn
# NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token

# ----------------------------------------
# API Configuration (OPTIONAL)
# ----------------------------------------
# If you have additional APIs or services
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
# NEXT_PUBLIC_CDN_URL=https://cdn.yourdomain.com

# ----------------------------------------
# Social Media & Deep Links (OPTIONAL)
# ----------------------------------------
# App scheme for deep linking (should match capacitor.config.ts)
NEXT_PUBLIC_APP_SCHEME=com.campus5.app

# Social media links
# NEXT_PUBLIC_INSTAGRAM_URL=https://instagram.com/yourhandle
# NEXT_PUBLIC_TWITTER_URL=https://twitter.com/yourhandle

# ----------------------------------------
# Notes for Mobile Deployment
# ----------------------------------------
# 1. All NEXT_PUBLIC_ variables are bundled into the app at build time
# 2. They are NOT secret - they will be visible in the compiled JavaScript
# 3. Never put sensitive keys (like service role keys) in NEXT_PUBLIC_ variables
# 4. After changing these values, you must rebuild the app:
#    - Run: npm run build:export
#    - Run: npx cap sync android (or ios)
#    - Rebuild in Android Studio / Xcode
# 5. For different environments (dev/staging/prod), use different .env files:
#    - .env.local (development, not committed)
#    - .env.production (production, not committed)
#    - .env.staging (staging, not committed)
